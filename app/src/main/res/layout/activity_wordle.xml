<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Wordle: 5 Harfli Kelimeyi Bul"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginBottom="30dp" />

    <!-- Tahmin Giriş Alanı (Hata veren etTahmin) -->
    <EditText
        android:id="@+id/etTahmin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="5 harfli tahmininizi yazın"
        android:maxLength="5"
        android:inputType="textCapCharacters"
        android:layout_marginBottom="10dp" />

    <!-- Tahmin Et Butonu (Hata veren btnTahminEt) -->
    <Button
        android:id="@+id/btnTahminEt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tahmin Et" />

    <!-- Sonuçların Görüneceği Alan (Opsiyonel ama kodda varsa lazım olur) -->
    <TextView
        android:id="@+id/tvSonuc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:textSize="16sp" />

</LinearLayout>